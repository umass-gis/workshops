source "https://rubygems.org"

gem "jekyll", "~> 4.3.2"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end

gem "just-the-docs", "0.6.2"  # pin to the current release
# gem "just-the-docs"  # pin to the latest release
